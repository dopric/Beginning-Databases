@model List<Employee>

<h1>Employees</h1>

<table>
    <tr>
        <td></td>
        <th>Name</th>
    </tr>
    @foreach(var employee in Model)
    {
        <tr>
            <td></td>
            <td>@employee.FirstName @employee.LastName</td>
        </tr>
    }
</table>